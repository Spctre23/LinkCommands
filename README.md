# Link Commands

A simple TShock plugin for creating custom commands that display web urls.

Supports the latest version of TShock: 6.1.0

# How to add commands
- Create a new entry with the same format as the default one provided in linkcommands.json.
